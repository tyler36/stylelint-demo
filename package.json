{
  "name": "stylelint-demo",
  "description": "A demo for Stylelint",
  "version": "1.0.0",
  "scripts": {
    "lint:css": "stylelint **/*.css",
    "lint:css:fix": "stylelint **/*.css --fix"
  },
  "devDependencies": {
    "stylelint": "^16.25.0",
    "stylelint-config-standard": "^39.0.1"
  }
}
